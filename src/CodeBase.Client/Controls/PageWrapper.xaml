<UserControl x:Class="CodeBase.Client.Controls.PageWrapper"
             x:Name="This"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CodeBase.Client.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">
    <DockPanel Background="#fff">
        <Label DockPanel.Dock="Top"
               Content="{Binding ElementName=This, Path=Title}"
               Visibility="{Binding ElementName=This, Path=TitleVisibility}"
               HorizontalContentAlignment="Center" 
               FontSize="15" 
               FontWeight="Bold" />
        <Grid x:Name="Container" DockPanel.Dock="Bottom">
            <Label>Some content</Label>
        </Grid>
    </DockPanel>
</UserControl>
