<UserControl x:Class="CodeBase.Client.Controls.ProgressOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CodeBase.Client.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">
    <Grid x:Name="FreezeOverlay" Background="#c000">
        <StackPanel VerticalAlignment="Center" 
                        HorizontalAlignment="Center">
            <TextBlock x:Name="FreezeText"
                    Text="Wait..."
                    TextWrapping="Wrap"
                    Foreground="#fff" 
                    FontSize="14"
                    Margin="10"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    TextAlignment="Center"></TextBlock>
            <ProgressBar Value="50" Height="10" Width="200"></ProgressBar>
        </StackPanel>
    </Grid>
</UserControl>
